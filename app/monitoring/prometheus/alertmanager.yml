global:
  resolve_timeout: 5m

route:
  receiver: default
  group_by: ["alertname"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: default
    # Replace with a real webhook (Slack, PagerDuty, email, etc.) before relying on this in production.
    # Example webhook receiver:
    # webhook_configs:
    #   - url: "https://hooks.example.com/alerts"
    #     send_resolved: true
